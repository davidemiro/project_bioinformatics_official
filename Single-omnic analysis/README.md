# Single Omnics Analysis
Before to execute the project, you must run the python script into **data-ready** folder to download the files. The python script will download and unzip the files automatically.
